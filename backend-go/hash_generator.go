/*package main

import (
	"fmt"
	"golang.org/x/crypto/bcrypt"
)

func main() {
	password := "Osvaldoderrache159159159+++[]" // tu contraseña deseada
	hash, err := bcrypt.GenerateFromPassword([]byte(password), bcrypt.DefaultCost)
	if err != nil {
		panic(err)
	}
	fmt.Println("🔑 Hashed password:", string(hash))
}
*/